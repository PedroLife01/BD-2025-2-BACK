# ============================================
# SIGEA Backend - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o arquivo .env no repositório!
# ============================================

# --------------------------------------------
# BANCO DE DADOS (PostgreSQL)
# --------------------------------------------
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/NOME_BANCO
# Exemplo local: postgresql://postgres:senha123@localhost:5432/sigea
DATABASE_URL="postgresql://postgres:sua_senha@localhost:5432/sigea?schema=public"

# --------------------------------------------
# SERVIDOR
# --------------------------------------------
# Porta onde o servidor Express vai rodar
PORT=3000

# Ambiente de execução (development | production | test)
NODE_ENV=development

# --------------------------------------------
# AUTENTICAÇÃO (JWT)
# --------------------------------------------
# Chave secreta para assinar tokens JWT
# IMPORTANTE: Use uma string longa e aleatória em produção!
# Gere uma com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="sua_chave_secreta_muito_segura_aqui_minimo_32_caracteres"

# Tempo de expiração do token (exemplos: 1h, 7d, 30d)
JWT_EXPIRES_IN="7d"

# --------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# --------------------------------------------
# URL do frontend Angular (separar múltiplas URLs com vírgula)
CORS_ORIGIN="http://localhost:4200"

# --------------------------------------------
# UPLOAD DE ARQUIVOS
# --------------------------------------------
# Tamanho máximo de arquivo em bytes (10MB = 10485760)
MAX_FILE_SIZE=10485760

# Tipos MIME permitidos para upload (separar com vírgula)
ALLOWED_MIME_TYPES="application/pdf"

# --------------------------------------------
# LOGS (Opcional)
# --------------------------------------------
# Nível de log (error | warn | info | debug)
LOG_LEVEL=info
